#!groovy

pipeline {
    agent none

    options {
        skipDefaultCheckout()
        timestamp()
    }

    stages {
        stage("Init") {
            steps {
                println "Init"
            }
        }
    }
}